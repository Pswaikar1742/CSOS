# HACK-THE-GAP-CSOS

## Quick Start

### A) Full System (Docker)
```bash
docker-compose up -d --build
```

### B) Vision Engine (Host)
```bash
source .venv/bin/activate && cd vision && python multi_stream_detector.py
```
